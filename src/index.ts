/**
 * @dev Biconomy class that is the entry point
 */
import EventEmitter from 'events';

export class Biconomy extends EventEmitter {
  constructor(provider: any, options) {
    // check and validate options
    _init();
  }

  _init() {
    // intersect JSON RPC Call
  }
}
